# annot-platform 开发注意事项（内部）

> 维护人：Edison | 2026-09-23 起

## 快照一致性（2026-09-23 批量删帧 bug 教训）

凡"扫描目录/查询列表后按索引（gi/idx）批量操作"的接口：
- **必须**在操作前取一次完整快照（如 `_frames()` 一次调用结果），全程基于该快照执行
- **禁止**每操作一个元素重新扫描/查询——文件被删后列表会变，索引整体漂移 → 跳帧、错删、越界
- 批量操作做成**单个批量接口**（如 `DELETE /api/ds/<id>/frames` body `{gis}`），不要前端循环调单条接口
- 前端展示序号（全局帧号 gi）与后端索引强耦合：任何会增删帧文件的操作完成后必须刷新帧列表再继续操作

## 后端改动生效方式

Python 路由在进程启动时加载：**改动 api/*.py 后必须重启 Windows 服务**
（`VMShare\SuperLabel\start_windows.bat`），前端 ui/*.js|html 每次现读、强刷即可。
